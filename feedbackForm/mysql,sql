-- CREATE TABLE emp (empid INTEGER NOT NULL PRIMARY KEY,
-- empname TEXT NOT NULL, email NOT NULL);

-- INSERT INTO emp(empid, empname, email) VALUES
-- (1,"Eloise","Eloise@wit.edu");
-- INSERT INTO emp(empid, empname, email) VALUES
-- (2,"Aya","Aya@wit.edu");

SELECT * from emp;